FROM postgres:15

# Install PostGIS and pgvector from the PGDG apt repository
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        postgresql-15-postgis-3 \
        postgresql-15-pgvector \
    && rm -rf /var/lib/apt/lists/*
