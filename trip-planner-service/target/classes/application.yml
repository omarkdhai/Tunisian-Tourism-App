server:
  port: 8083

spring:
  application:
    name: trip-planner-service
  config:
    import: optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:8888
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/trip_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:9090/realms/tunisia-tourism}
          jwk-set-uri: ${KEYCLOAK_JWK_URI:http://localhost:9090/realms/tunisia-tourism/protocol/openid-connect/certs}

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
  instance:
    prefer-ip-address: true

app:
  place-service-url: http://place-service
  ai-service-url: http://localhost:8000

springdoc:
  api-docs:
    path: /api/trips/v3/api-docs
  swagger-ui:
    path: /api/trips/swagger-ui.html
